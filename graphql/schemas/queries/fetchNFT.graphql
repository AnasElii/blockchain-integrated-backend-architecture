query getAllNFTs($id: ID!, $sortField: String, $sortOrder: Int, $limit: Int, $offset: Int) {
    nft(id: $id, sortField: $sortField, sortOrder: $sortOrder, limit: $limit, offset: $offset) {
        nftID
        name
        description
        owner
        price
        image
    }
}